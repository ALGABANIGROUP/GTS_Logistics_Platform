services:
  - type: web
    name: GTS-Logistics-API
    runtime: python
    plan: starter
    region: oregon
    rootDir: .
    branch: main
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health/ping
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
