/**
 * Freight Bookings (IT-Freight) Bot Page
 * Page wrapper for the Freight Bookings interface
 */
import { FreightBookingsInterface } from "../../components/bots";

export default function AIFreightBookingsBot() {
    return <FreightBookingsInterface mode="active" />;
}
